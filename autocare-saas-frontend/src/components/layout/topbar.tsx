'use client';
import { Bell, ChevronDown, Moon, Sun } from 'lucide-react'; import { useTheme } from 'next-themes'; import { usePathname } from 'next/navigation';
export function Topbar({ email }: { email: string }): React.JSX.Element { const { resolvedTheme, setTheme } = useTheme(); const pathname = usePathname(); const title = pathname.split('/').filter(Boolean).at(-1)?.replaceAll('-', ' ') ?? 'Dashboard'; return <header className="flex h-16 items-center justify-between border-b bg-white px-6 dark:bg-slate-950"><div><p className="text-xs text-slate-500">Workspace</p><h1 className="capitalize font-semibold">{title}</h1></div><div className="flex items-center gap-3"><button onClick={() => setTheme(resolvedTheme === 'dark' ? 'light' : 'dark')} className="rounded-lg p-2 text-slate-500 hover:bg-slate-100 dark:hover:bg-slate-900" aria-label="Toggle color theme">{resolvedTheme === 'dark' ? <Sun className="size-4" /> : <Moon className="size-4" />}</button><button className="relative rounded-lg p-2 text-slate-500 hover:bg-slate-100 dark:hover:bg-slate-900" aria-label="Notifications"><Bell className="size-4" /><span className="absolute right-1.5 top-1.5 size-1.5 rounded-full bg-blue-600" /></button><div className="hidden items-center gap-2 border-l pl-3 sm:flex"><span className="grid size-8 place-items-center rounded-full bg-blue-100 text-xs font-bold text-blue-700">{email.slice(0, 1).toUpperCase()}</span><span className="max-w-40 truncate text-sm font-medium">{email}</span><ChevronDown className="size-3 text-slate-400" /></div></div></header>; }
